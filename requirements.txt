# Versions below are reasonable-as-of-now pins, not guaranteed latest —
# if `pip install -r requirements.txt` complains a version doesn't exist,
# drop the ==version and let pip grab current, then re-pin what you get.

# Core RDF
rdflib>=7.6.0        # was pinned to 7.1.1 — that version is incompatible with
                      # Python 3.14 (AttributeError on rdflib.resource.Resource
                      # at import time). 7.6.0+ confirmed working.
pyoxigraph==0.5.3     # Python bindings for Oxigraph — lets you query/load
                      # the store directly from Python instead of shelling
                      # out to the CLI, useful for the promote_to_rdf script

# Staging DB for the extraction review workflow
duckdb==1.5.4

# Transcript acquisition + extraction
requests==2.32.3
beautifulsoup4==4.12.3
pdfplumber==0.11.4

# Named entity recognition (rough first-pass indexing of PDFs — see
# extraction/index_named_entities.py). Note: pip install alone does NOT
# include the language model — after installing, also run:
#   python -m spacy download en_core_web_sm
# Unpinned (not ==3.8.14 like earlier) after that exact version turned out
# not to exist for Python 3.14 — same class of issue as the rdflib/3.14
# problem earlier in this project, different package.
spacy>=3.8.0
click>=8.0.0  # spacy's CLI (used for `python -m spacy download`) imports this
              # directly; on Python 3.14 it wasn't reliably pulled in as a
              # transitive dependency via typer, so pinning it explicitly

# LLM-assisted extraction (optional — only needed if you call the API
# directly from Python rather than doing extraction passes in-chat)
anthropic==0.40.0

python-dotenv==1.0.1
